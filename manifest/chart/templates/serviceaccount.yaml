{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "glassbox-mol-audit.serviceAccountName" . | quote }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    {{- include "glassbox-mol-audit.labels" . | nindent 4 }}
  annotations:
    {{- if and .Values.workloadIdentity.enabled .Values.workloadIdentity.gcpServiceAccount }}
    iam.gke.io/gcp-service-account: {{ .Values.workloadIdentity.gcpServiceAccount | quote }}
    {{- end }}
    {{- range $k, $v := .Values.serviceAccount.annotations }}
    {{ $k }}: {{ $v | quote }}
    {{- end }}
{{- end }}
